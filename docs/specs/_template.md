# 功能名称

## 状态

- 当前状态：待确认
- 日期：
- 风险等级：XS / S / M / L

## 背景

说明为什么需要这个功能，以及它解决什么问题。

## 已确认事实

- 只记录当前代码、测试、运行结果或用户确认能够证明的事实。
- 无法证明的业务含义写入“假设与待业务确认”。

## 目标

- 用户可以……
- 系统可以……

## 范围

- 本次会修改……
- 本次会影响……

## 非目标

- 本次不做……
- 本次不引入……

## 用户流程

1. 用户打开……
2. 用户执行……
3. 系统展示或保存……

## 核心流程与关键异常场景

- 成功路径：
- 失败路径：
- 取消或中断：
- 数据缺失或兼容行为：

## 关键问题与回答

| 问题 | 状态 | 当前结论 | 依据 | 对范围的影响 |
| --- | --- | --- | --- | --- |
| 示例问题 | 待确认 | 待业务确认 | 无 | 未确认前不进入 PLAN |

状态只允许：待确认、已确认、不适用、推迟到后续 Spec。

## 决策记录及被放弃方案

- 采用方案：
- 被放弃方案及原因：

## 假设与待业务确认

- 假设：
- 待业务确认：

## 数据模型

- 涉及类型：
- 新增字段：
- 迁移策略：
- 兼容旧数据方式：

如果不涉及持久化或共享类型，写明“本次不涉及数据模型变化”。

## UI 结构

说明涉及 `apps/studio/src/pages`、`components`、`constants`、`styles` 等目录的页面和组件边界。

## 技术方案

说明会修改哪些模块、为什么这样拆，以及跨包依赖如何流动。

## TDD 测试点

- [ ] 先写失败测试：
- [ ] 覆盖成功路径：
- [ ] 覆盖边界情况：
- [ ] 覆盖迁移或异常路径：

如果是 docs-only 变更，写明“本次不涉及业务测试，使用文档验证清单”。

## 验收标准

- [ ] 用户流程可以完整走通。
- [ ] 目录结构符合 `AGENTS.md`。
- [ ] 功能现状文档已同步检查。
- [ ] 类型检查通过。
- [ ] 必要测试通过。
- [ ] 构建通过。

## 验收示例

1. 给定明确前置条件，当用户执行关键操作时，系统产生可观察结果。
2. 给定关键失败条件，系统保持既定数据边界并展示可验证反馈。

## DEFINE 完成检查

- [ ] 没有会改变核心方案的待确认项。
- [ ] 范围与非目标明确且没有冲突。
- [ ] 主流程和关键异常场景可以验收。
- [ ] 数据模型、Feature 和 ADR 影响已经判断。
- [ ] 成功标准可通过测试、命令或明确人工步骤验证。

所有适用项通过并经用户确认后，才能把状态改为“已确认”并进入 PLAN。XS/S 且行为明确的修改可以使用对话内 Micro Spec，但必须保留外部行为、失败行为和验证方式；M/L 或高风险边界使用完整 Spec。

## 验证命令

```bash
pnpm run lint
pnpm run typecheck
pnpm run test
pnpm run build
```

## 文档同步

- [ ] `docs/features/*.md`
- [ ] `docs/plans/YYYY-MM/*.md`
- [ ] `docs/adr/*.md`
- [ ] 不需要同步，原因：

## 完成记录

- 完成时间：
- 实际完成内容：
- 验证结果：
- 评审结果：
- commit：
- 未覆盖风险：
- 关联 Feature / Plan / ADR：
